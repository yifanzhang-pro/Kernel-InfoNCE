# Kernel-InfoNCE on MoCo

## Installation

Based on kernel-infoNCE's environment, run `pip install lightly`

## Run Experiment

`python moco.py [args]`

The meaning of different arguments is same to Kernel-InfoNCE's arguments. 



